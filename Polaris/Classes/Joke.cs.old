using System.Collections.Generic;
using Newtonsoft.Json;
using Polaris.Enums;

namespace Polaris.Classes
{
    public class Joke
    {
        [JsonProperty]
        public int id;
        [JsonProperty]
        private string type;
        [JsonProperty]
        public string joke;
        [JsonProperty]
        public string answer;
        
        public static string token { get; } = System.IO.File.ReadAllText(@"C:\Users\Lexa\Desktop\JokeToken.txt");
        
        
        // TODO Remove static abuse here
        public static readonly Dictionary<string, string> Sentences = new()
        {
            {"global", "en tout genre"},
            {"dev", "de développeur"},
            {"dark", "sur les noirs"},
            {"limit", "assez hard"},
            {"beauf", "beauf"},
            {"blondes", "sur les blondes"}
        };
        
        // TODO Remove static abuse here
        private static readonly Dictionary<string, JokeType> Types = new()
        {
            {"global", JokeType.Global},
            {"dev", JokeType.Dev},
            {"dark", JokeType.Dark},
            {"limit", JokeType.Limit},
            {"beauf", JokeType.Beauf},
            {"blondes", JokeType.Blondes}
        };

        public JokeType Type()
        {
            return Types[type];
        }
    }
}